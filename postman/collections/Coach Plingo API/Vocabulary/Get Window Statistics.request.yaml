$kind: http-request
description: Get statistics about the active learning window
url: "{{base_url}}/vocabulary/window-stats/{{learning_path_id}}"
method: GET
headers:
  Authorization: Bearer {{access_token}}
order: 2000
