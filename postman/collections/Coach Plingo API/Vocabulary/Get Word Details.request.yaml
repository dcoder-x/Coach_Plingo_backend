$kind: http-request
description: Get detailed information about a specific word including
  pronunciation and images
url: "{{base_url}}/vocabulary/word/{{word_id}}"
method: GET
headers:
  Authorization: Bearer {{access_token}}
order: 3000
